apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nodeapp
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: nodeapp
          servicePort: 80
